services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: >
      sh -c "uv run python manage.py migrate &&
             uv run gunicorn django_project.wsgi:application --bind 0.0.0.0:8000"
